{
  "name": "@pingpay/onramp-api",
  "scripts": {
    "build": "echo 'Cloudflare Workers: No build needed, Wrangler handles bundling'",
    "dev": "wrangler dev --port 8787",
    "deploy": "CLOUDFLARE_API_TOKEN=$CLOUDFLARE_API_TOKEN wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "^1.21.0",
    "@hono/zod-validator": "^0.7.0",
    "@pingpay/onramp-types": "workspace:*",
    "hono": "^4.8.3",
    "hono-sessions": "^0.7.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250628.0",
    "wrangler": "^4.4.0"
  }
}
